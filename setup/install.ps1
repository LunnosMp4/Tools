# For Windows
# Install all Requiered Packages

pip install requests
pip install jsbeautifier
pip install img2pdf
pip install pypdf2
pip install torch
pip install pillow
pip install pydub
pip install ffmpeg # You may need to install it from here https://www.ffmpeg.org/download.html and add it to the PATH